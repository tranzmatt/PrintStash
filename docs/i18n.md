# Frontend localization

All application copy belongs to the catalogs in `frontend/src/locales`. The English catalog defines message identifiers; every supported language must provide the same identifiers and interpolation parameters. Existing descriptive identifiers and explicit source-message identifiers share one catalog. English source text inside `uiText(...)` is a checked message identifier, not an untranslated DOM fallback.

Use `useI18n().t(key, values)` in React or `uiText(key, values)` in display helpers. Components using imperative helpers subscribe through `useUiLocale()` so a language change updates their output, including dialogs rendered in portals. Keep user names, tags, filenames, paths, descriptions and license content as interpolation values or ordinary React text; never pass them through a phrase translator.

Messages containing a quantity can provide CLDR `zero`, `one`, `two`, `few`, `many` and `other` forms. `other` is required. Pass a numeric `count`; do not concatenate English plural suffixes. Parameters are inserted in one pass after plural selection, so braces in user input remain literal.

Display formatters accept a locale in the domain package. Application wrappers supply the selected language, independently of the browser language. Numeric form values and machine-readable API fields retain their wire format.

API error codes are resolved to translated messages at the presentation boundary. Unknown errors use a translated fallback. Provider labels and help text are explicitly identified as system-owned UI before translation. Task message descriptors retain their identifiers and parameters for translation when displayed.

The UI no longer relies on a MutationObserver rewriting arbitrary text nodes. Localized copy must work on its first React render and inside portals.

## Adding a language

1. Copy `frontend/src/locales/en.json` to a catalog named with its BCP 47 language tag and translate every message. Preserve interpolation names. Translate system-owned labels and abbreviated card labels too; keep brands and standard units when appropriate.
2. Import the catalog in `frontend/src/locales/catalogs.ts` and add its native name, direction and messages to `localeDefinitions`. The menu and offline/manifest assets derive from this registry.
3. Use the CLDR plural categories needed by the language, always including `other`. Run the catalog and JSX guard tests, typecheck and the localization browser flow.
4. Review narrow screens, long translations and screen-reader labels in the new language. For an RTL language, verify layout direction in the browser before releasing it; setting the document direction alone is not an RTL layout audit.

Technical diagnostics, filenames, user descriptions and provider-owned configuration dumps are data and remain unchanged. New provider-authored UI labels need corresponding catalog entries; unknown API errors use localized recovery copy.

## Validation matrix

Focused checks exercise the observable behaviors below. The repository guard also checks every registered catalog and rejects untranslated JSX/accessibility literals. Full-suite and browser results are recorded in the PR.

| # | Behaviour (test name) | Category | Precondition / input | Observable outcome asserted | Tier | Status |
|---|---|---|---|---|---|---|
| 1 | preserves user text matching a message identifier | Edge | A model named Files; Spanish UI | Model name remains Files | Frontend unit | ✅ `src/lib/__tests__/i18n.test.tsx::preserves user text matching a message identifier` |
| 2 | updates explicit messages after selecting a language | Happy | English surface switched to Spanish | Visible messages update without DOM rewriting | Frontend unit | ✅ `src/lib/__tests__/i18n.test.tsx::updates explicit messages after selecting a language` |
| 3 | localizes card metric abbreviations | Happy | Spanish card with file count metric | Localized abbreviation visible | Frontend unit | ✅ `src/components/__tests__/model-card.test.tsx::localizes card metric presentation` |
| 4 | localizes favorite accessible names | Happy | Spanish card | Accessible action contains translated verb and original model name | Frontend unit | ✅ `src/components/__tests__/model-card.test.tsx::localizes card actions while preserving the model name` |
| 5 | localizes tag accessible names | Happy | Spanish editable card | Accessible tag action translated | Frontend unit | ✅ `src/components/__tests__/model-card.test.tsx::localizes card actions while preserving the model name` |
| 6 | formats values using the selected locale | Edge | Browser and application languages differ | Date and number output follows the application | Frontend unit | ✅ `src/lib/__tests__/format.test.ts::formats values using the selected locale independently of the browser` |
| 7 | selects CLDR plural forms | Edge | English, Spanish and a language with multiple plural categories | Correct message form for each count | Frontend unit | ✅ `src/lib/__tests__/locale.test.ts::selects $locale plural for $count` |
| 8 | preserves parameter text containing placeholders | Edge | User name contains braces and dollar signs | Name is inserted verbatim once | Frontend unit | ✅ `src/lib/__tests__/locale.test.ts::preserves parameter text containing placeholders` |
| 9 | localizes API error codes | Error | Known code in Spanish | Translated error returned | Frontend unit | ✅ `src/lib/__tests__/errors.test.ts::localizes error recovery` |
| 10 | localizes unknown errors | Error | Unrecognized API code | Translated recovery message | Frontend unit | ✅ `src/lib/__tests__/errors.test.ts::localizes error recovery` |
| 11 | updates stored task messages on language change | Happy | Task created in English, displayed in Spanish | Stored message parameters rendered in Spanish | Frontend unit | ✅ `src/lib/__tests__/task-center.test.ts::updates stored task messages on language change without translating user names` |
| 12 | renders all registered languages as choices | Happy | Language registry | Each language has a reachable control | Frontend unit | ✅ `src/components/__tests__/locale-toggle.test.tsx::renders all registered languages as choices` |
| 13 | preserves language when persistence is unavailable | Error | localStorage write throws | Active selection still works | Frontend unit | ✅ `src/lib/__tests__/i18n.test.tsx::preserves language when persistence is unavailable` |
| 14 | rejects untranslated JSX copy | Error | New literal or accessibility attribute | Repository guard reports the source | Frontend unit | ✅ `tests/repo/i18n-coverage.test.ts::rejects untranslated UI copy` |
| 15 | validates every catalog's parameters | Edge | All registered languages | Matching keys and parameter sets | Frontend unit | ✅ `tests/repo/i18n-coverage.test.ts::validates every key, plural form and parameter in %s` |
| 16 | localizes the offline shell | Error | Cached offline document | Heading, explanation, retry and language translated | Playwright | ✅ `tests/e2e/i18n.spec.ts::localizes the offline shell without an API or network` |
| 17 | preserves language after reload | Happy | User chooses Spanish and reloads | Settings, accessible labels and locale remain Spanish | Playwright | ✅ `tests/e2e/i18n.spec.ts::language selection survives reload at 1280px / 390px` |
| 18 | localizes its default accessible name | Happy | Spanish loading indicator | Status is named Cargando | Frontend unit | ✅ `src/components/ui/__tests__/spinner.test.tsx` |
| 19 | localizes controlled revision states | Happy | Spanish revision facet | Known-good label is translated | Frontend unit | ✅ `src/lib/__tests__/filter-labels.test.ts` |
| 20 | preserves user-authored facet values | Edge | Printer/material matching a catalog key | Value remains unchanged | Frontend unit | ✅ `src/lib/__tests__/filter-labels.test.ts` |
| 21 | search shortcut works with localized accessible labels | Happy | Spanish library; slash shortcut | Search textbox gains focus | Playwright | ✅ `tests/e2e/i18n.spec.ts` |
| 22 | localizes installation metadata | Happy | Each registered language | Manifest description and direction match the language | Frontend unit | ✅ `tests/repo/localization-assets.test.ts::provides installation metadata in %s` |
| 23 | localizes current release notes | Happy | About displays the latest release | Displayed release date and notes have catalog entries | Frontend unit | ✅ `tests/repo/i18n-coverage.test.ts::localizes the release notes displayed in About` |
| 24 | preserves selection with the real backend | Happy | Spanish selected in settings, then reload | Settings navigation remains Spanish | Playwright real backend | ✅ `tests/e2e-real/i18n.spec.ts::switching to Spanish translates settings navigation and survives a reload` |
| 25 | maps a known storage availability code | Happy | Backend returns `storage_dependency_missing` | Full-image requirement message is selected | Frontend unit | ✅ `src/lib/__tests__/storage-operations.test.ts::maps a known availability code` |
| 26 | uses localized recovery copy for an unknown storage code | Error | Backend returns a future storage reason | Localized storage fallback is selected | Frontend unit | ✅ `src/lib/__tests__/storage-operations.test.ts::uses localized recovery copy for an unknown code` |
| 27 | localizes coded unavailable-provider reasons | Error | Spanish picker receives an unavailable vault-use code | Disabled provider has a translated accessible reason | Frontend unit | ✅ `src/components/__tests__/storage-provider-picker.test.tsx::localizes coded unavailable-provider reasons` |
| 28 | refuses storage checks for unavailable providers | Error | Setup receives a coded unavailable provider or a removed provider | Localized alert appears; no storage or setup request is sent | Frontend unit | ✅ `src/pages/__tests__/setup.test.tsx::refuses a storage check for an %s provider` |
| 29 | updates empty Task Center copy after a language change | Happy | Empty English Task Center switched to Spanish | Empty-state copy updates in place | Frontend unit | ✅ `src/components/__tests__/task-list.test.tsx::updates empty-state copy after a language change` |
| 30 | localizes completed task summaries | Happy | Completed task with persisted message descriptors | Title, detail, status and clear action render in Spanish | Frontend unit | ✅ `src/components/__tests__/task-list.test.tsx::localizes completed task summaries` |
| 31 | localizes failed task recovery controls | Error | Failed item with coded error and available recovery actions | Error, disclosure and recovery actions render in Spanish | Frontend unit | ✅ `src/components/__tests__/task-list.test.tsx::localizes failed task recovery controls` |
| 32 | retains failed item names literally | Edge | Filename contains braces and replacement symbols | Filename remains byte-for-byte visible after disclosure | Frontend unit | ✅ `src/components/__tests__/task-list.test.tsx::retains failed item names literally` |
| 33 | localizes indefinite active progress copy | Edge | Running task has no discovered total | Spanish progress explanation and state are visible | Frontend unit | ✅ `src/components/__tests__/task-list.test.tsx::localizes indefinite active progress copy` |
| 34 | defaults to English when locale persistence cannot be read | Error | `localStorage.getItem` throws | Locale lookup returns English without throwing | Frontend unit | ✅ `src/lib/__tests__/locale.test.ts::defaults to English when locale persistence cannot be read` |
| 35 | falls back to the status for structured validation details | Error | HTTP 422 carries FastAPI field-error objects | Parsed error retains status and safe `422` code | Frontend unit | ✅ `src/lib/__tests__/errors.test.ts::falls back to the status string for structured validation details` |
| 36 | translates legacy controlled task titles | Edge | Persisted task predates message descriptors | Known controlled title renders in Spanish | Frontend unit | ✅ `src/lib/__tests__/task-center.test.ts::translates legacy controlled task titles` |
| 37 | localizes failed task error details | Error | Persisted failed task contains a known error code | Detail renders as the Spanish recovery message | Frontend unit | ✅ `src/lib/__tests__/task-center.test.ts::localizes failed task error details` |
| 38 | links the default localized web manifest | Happy | English document initial load | Manifest link selects the English asset and the linked manifest has the installable shape | Playwright | ✅ `tests/e2e/pwa.spec.ts::the web app manifest is served with the expected shape` |
